<%= form_for(@patient_history) do |f| %>
  <% if @patient_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@patient_history.errors.count, "error") %> prohibited this patient_history from being saved:</h2>

      <ul>
      <% @patient_history.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :patient %><br />
    <%= f.text_field :patient %>
  </div>
  <div class="field">
    <%= f.label :history %><br />
    <%= f.text_field :history %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
