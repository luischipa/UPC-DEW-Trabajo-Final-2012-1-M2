<h1>Tweetchive</h1>

<!--twitter = Twitter::Base.new(@username,@password)-->

<%= link_to 'New Tweet', new_tweet_path %>

<table>

	<tr>
		<th>Tweets</th>
		<th></th>
		<th></th>
		<th></th>
	</tr>

	<% @tweets.each do |tweet| %>
	  <tr>
		<td><%= tweet.status %></td>
	  </tr> 
	  <tr>
		<td><%= tweet.read_text%></td>
	  </tr>  
	  <tr>
		<td><%= link_to 'Edit', edit_tweet_path(tweet) %>
		<%= link_to 'Destroy', tweet, confirm: 'Are you sure?', method: :delete %></td>
	  </tr>
	  <tr>
	  
	  </tr>
	<% end %>

</table>

<br />


